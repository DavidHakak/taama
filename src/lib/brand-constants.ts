/**
 * קבועים בלבד, בלי שום ייבוא — כדי ש-client components יוכלו לקרוא
 * אותם מבלי לגרור את חיבור ה-DB לבנדל של הדפדפן.
 * @/lib/brand מייבא מכאן, לא להפך.
 */

/**
 * המותג שהחנות הציבורית מגישה כרגע.
 * זמני: כשיהיו שני דומיינים, המותג ייגזר מה-Host ב-proxy.ts.
 */
export const STOREFRONT_BRAND_SLUG = 'taama'
